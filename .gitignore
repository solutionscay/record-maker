/target
**/*.rs.bk

# Tauri build output + generated capability schemas (source icons ARE tracked)
/src-tauri/target/
/src-tauri/gen/schemas/

# local solution data created at runtime (app.db / data.db), wherever launched
.rm-data/

# Node (frontend tooling, added later)
node_modules/
dist/
